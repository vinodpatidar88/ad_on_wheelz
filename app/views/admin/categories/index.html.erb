<div class="d-flex justify-content-between align-items-center pt-3 pb-2 mb-3 border-bottom border-secondary">
  <h1 class="h2 text-white">Categories</h1>
  <%= link_to 'New Category', new_admin_category_path, class: 'btn btn-primary' %>
</div>

<div class="admin-card">
  <div class="table-responsive">
    <table class="table">
      <thead>
        <tr>
          <th>ID</th>
          <th>Name</th>
          <th>Description</th>
          <th style="width: 150px;">Actions</th>
        </tr>
      </thead>
      <tbody>
        <% if @categories.any? %>
          <% @categories.each do |category| %>
            <tr>
              <td><%= category.id %></td>
              <td class="fw-bold text-white"><%= category.name %></td>
              <td><%= category.description %></td>
              <td>
                <div class="d-flex gap-2">
                  <%= link_to 'Edit', edit_admin_category_path(category), class: 'btn btn-outline-warning btn-sm' %>
                  <%= button_to 'Delete', admin_category_path(category), method: :delete, data: { turbo_confirm: 'Are you sure?' }, class: 'btn btn-outline-danger btn-sm' %>
                </div>
              </td>
            </tr>
          <% end %>
        <% else %>
          <tr>
            <td colspan="4" class="text-center text-muted py-4">No categories created yet.</td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
